#ifndef STORY_CAT_H
#define STORY_CAT_H

#include <stddef.h>
#include "story.h"  // for Node struct

// Declare the item_at_node array
extern const char *item_at_node[];
extern const size_t item_at_node_length;

extern Node cat_story[];
extern int cat_story_length;

#endif
