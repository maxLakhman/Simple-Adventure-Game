#include "story_cat.h"

// Define the story nodes for "The Memory of Ash"
Node cat_story[] = {
  {"You wake up cold...", "Stand", "Whisper", 1, 2},
  // ... rest of the ash story
};

int cat_story_length = sizeof(cat_story) / sizeof(Node);
